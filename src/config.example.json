{
	"token": "token goes here",
	"client_id": "client id goes here",

	"commands": {
		"blender_base_command": "blender -b {BLEND_FILE} -P {PYTHON_SCRIPT_FILE} -- --overlay-path {OVERLAY_PATH} --output-path {OUTPUT_PATH} --jpeg-quality {JPEG_QUALITY} --cycles-device CPU"
	}
}
